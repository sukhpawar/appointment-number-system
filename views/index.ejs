<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link href="style.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css?family=Roboto:400,500,700,900&display=swap" rel="stylesheet">
    <title>Walk In customer System</title>
</head>
<body>
    <div class="main-area">
      <div>
        <h1>Customer Details</h1>
         <table>
             <tr>
                 <th>#</th>
                 <th>Name</th>
                 <th>Phone Number</th>
                 <th>Time In</th>
                 <th>Served or Not</th>
                 <th>Add Comment</th>
                 <th>Comment</th>
                 
                 
             </tr>
             <% customers.forEach(customer=>{ %>
               <tr>
                   <td><%= customer.seqNumber %></td>
                   <td><%= customer.name %></td>
                   <td><%= customer.mobile %></td>
                   <td ><%= customer.time %></td>
                   <td>
                       <form action="/served" method="POST">
                       <button name="isServed" value="<%= customer._id %>" >Served</button>
                       </form>
                    </td>
                    <td>
                       <form action="/getAddCommentForm" method="post">
                        <button class="addComment-class" name="addCommentId" value="<%= customer._id %>">Comment</button>
                    </form>
                  </td>
                  <td>
                     <%= customer.comment %>
                  </td>
               </tr>
               
            <% }) %>
         </table>
         <div style="margin-top: 20px; margin-bottom: 20px;"> <a href="/addCustomerForm">Add new Customer</a></div>
        
    </div>
   
</div>

    
</body>

</html>